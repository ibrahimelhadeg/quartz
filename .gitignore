.DS_Store
.gitignore
node_modules
public
prof
tsconfig.tsbuildinfo
.obsidian
.quartz-cache
private/
.replit
replit.nix
.quartz/
quartz/cli/tui/dist/

# Local pnpm setup byproducts. npm is the fork's canonical package
# manager (loader calls npm; package-lock.json is tracked); these are
# never versioned upstream.
pnpm-lock.yaml
pnpm-workspace.yaml
quartz.config.yaml
